require.config({
	baseUrl: './libs',
	paths: {
		jquery:"jquery",
		API: 'API',
		request: 'request',
		data: '../components/data'
	}
})
define(['jquery','data'],function($,data){
	var $ = $,
		data = data;
})
